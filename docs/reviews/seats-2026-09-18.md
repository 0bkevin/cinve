# Tarifas, asientos y caramelería — 18/09/2026

## Cambio de Cinex

La consulta autenticada de Sambil Chacao (`SBC`, función `23062`) redirigió de `/boletos.php` a `/boletosdev.php`, conservando sede y función. No era un fallo de autorización. Se admite exactamente un salto a esa ruta en el mismo origen, con parámetros idénticos, nueva lectura de sesión y plazo HTTP compartido. Los demás destinos continúan rechazados.

El servicio corregido devolvió seis tarifas. Adulto Dig: 3.394,20 VES (boleto 848,55 y otros cargos 2.545,65), sin verificar total final de compra.

## Asientos

- Cinepic: la página pública de compra incluye `butacasData.butacas`. El bundle `1018-28b7501a8253d37f.js` usa `libre=1` para libre y `0` para ocupada; `seleccionada=1` en la respuesta no representa una selección del usuario. Las etiquetas `nombre_butaca` pueden diferir de la fila/columna física. Se conservan ambas. El tipo de tarifa determina elegibilidad adicional.
- Cines Unidos: su bundle `6221-387e904905fdba32.js` usa GET `/api/seats?theaterId=…&showTimeId=…` con Bearer. Respondió 200 autenticado, sin crear una orden. El POST posterior `/tickets/www/theaters/…/sessions/…/orders` no se ejecutó. Estado 0 se muestra libre; estados especiales se mantienen restringidos o desconocidos, con el código original.
- Cinex: GET autenticado `/asientos.php` después de leer tarifas devolvió HTTP 200 pero un error explícito al obtener el mapa, sin posiciones de asientos. No se ejecutaron formularios de selección de boletos, condiciones, asientos o reservas. No se anuncia soporte de mapas Cinex.

Validación en vivo del servicio a las 14:40–14:41 de Caracas:

| Proveedor / sede | Función | Resultado |
|---|---|---|
| Cinepic Candelaria | 23415 / película 913, 16:00 | Mapa, 328 butacas marcadas libres; tarifas disponibles |
| Cinepic VVIP | 3072 / película 180, 16:00 | Mapa, 60 butacas marcadas libres; tarifa disponible |
| Cines Unidos Sambil Caracas | 49988 / Toy Story 5, 16:40 | Mapa, 131 asientos ordinarios libres; estados especiales preservados |

Son fotografías del momento. Los contadores de entradas Cinepic difieren del mapa (VVIP: 57 frente a 60); se devuelve una advertencia y no se promete ese cupo de venta. El ASCII preserva coordenadas, espacios y etiquetas; no afirma orientación de pantalla. Lecturas sin caché local ni órdenes, y límites de tamaño/validación para evitar mapas inventados o desmesurados.

## Caramelería

Se revisaron los catálogos actuales usando Cinve primero. Sambil Caracas (Cines Unidos) devolvió 56 productos con USD/VES y stock. Ejemplo leído del catálogo oficial: Combo tequeños plus, 2 bebidas grandes y caja de 15 tequeños, 19,40 USD / 16.461,87 VES. No se añadieron productos al carrito ni se verificó el total de checkout.

Cinepic Candelaria y VVIP devolvieron `unidades_negocios: []` por API; la página VVIP también incluyó `candyData` vacío. No equivale a ausencia de caramelería en taquilla. Cinex conserva el lector autenticado de productos y combos verificado en la revisión de login del mismo día.

## Verificación

Typecheck, build y 97 pruebas, incluidos MCP, sesiones alojadas, mapa/ASCII, estados desconocidos, posiciones duplicadas, identidad de función, caché y redirecciones permitidas/rechazadas. No se guardaron contraseñas, cookies, tokens ni respuestas de perfil en el repositorio.
