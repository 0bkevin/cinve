// Shared by MCP instructions, status and the transport challenge. OAuth client
// authorization is distinct from the local cinema-password CLI.
export const hostedAuthInstructions = 'Para conectar un cine, llama connect_account con el proveedor. Si aparece una solicitud de autorización de Cinve, inicia tú la acción OAuth nativa del cliente cuando esté disponible. En Codex local, si puedes ejecutar comandos en el equipo y la configuración del usuario, ejecuta codex mcp login cinve (usa el nombre real configurado si es distinto), usa una sesión de terminal cancelable, mantén el proceso activo y presenta su URL como enlace clicable en una actualización intermedia. No ocultes la URL capturando toda la salida hasta que el comando termine ni uses un timeout corto para esperar la aprobación humana. Observa la finalización de la autorización y continúa sin pedir al usuario que escriba listo. Si cancela o pide otro enlace, detén el intento anterior; no reinicies un login cancelado salvo que lo pida. Después del login comprueba connection.client_authorized con get_auth_status: false significa que esta conversación sigue siendo anónima, no que falló el login del cine. Usa la acción nativa de refrescar conexiones si tu cliente la expone (en integraciones Codex: config/mcpServer/reload), luego reintenta connect_account. No inventes herramientas ni reinicies otra aplicación. Si no tienes esa acción y sigue siendo anónima, explica una sola vez que debe reconectar Cinve para cargar la autorización; no vuelvas a pedir login ni busques documentación. No envíes al usuario a buscar botones en Configuración ni a ejecutar comandos si puedes iniciar tú la autorización. No ejecutes ese comando en un entorno remoto ajeno a la instalación del usuario. Solo cuando no tengas acceso a una acción OAuth nativa ni a su CLI local, explica esa limitación y usa el control de conexión del cliente. No busques instrucciones ni enlaces de acceso en la web para este flujo. El comando codex mcp login autoriza el asistente y no pide la contraseña del cine. Nunca ejecutes el CLI local de credenciales del cine en modo alojado. Solo el usuario introduce las credenciales del cine en el enlace privado que devuelve connect_account.';
