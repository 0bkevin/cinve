// Shared by MCP instructions, status and the transport challenge. OAuth client
// authorization is distinct from the local cinema-password CLI.
export const hostedAuthInstructions = 'Para conectar un cine, llama connect_account con el proveedor. Si aparece una solicitud de autorización de Cinve, inicia tú la acción OAuth nativa del cliente cuando esté disponible. En Codex local, si puedes ejecutar comandos en el equipo y la configuración del usuario, ejecuta codex mcp login cinve (usa el nombre real configurado si es distinto), mantén el proceso activo y presenta su URL como enlace clicable. Espera a que finalice la autorización y reintenta connect_account; si la sesión actual no carga las nuevas credenciales, informa que debe reconectar Cinve. No envíes al usuario a buscar botones en Configuración ni a ejecutar comandos si puedes iniciar tú la autorización. No ejecutes ese comando en un entorno remoto ajeno a la instalación del usuario. Solo cuando no tengas acceso a una acción OAuth nativa ni a su CLI local, explica esa limitación y usa el control de conexión del cliente. No busques instrucciones ni enlaces de acceso en la web para este flujo. El comando codex mcp login autoriza el asistente y no pide la contraseña del cine. Nunca ejecutes el CLI local de credenciales del cine en modo alojado. Solo el usuario introduce las credenciales del cine en el enlace privado que devuelve connect_account.';
